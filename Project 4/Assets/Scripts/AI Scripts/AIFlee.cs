using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AIFlee : MonoBehaviour
{
    // This CS file is intended in testing the Fleeing algorithm for the AI tank
    // All the code seen below has been moved to AIController.cs
    // Which is why all the commented code will be in the AIControll.cs

    //
    //private UnityEngine.AI.NavMeshAgent agent;

    //
    //public GameObject Player;

    //
   // public float EnemyDistance = 4.0f;

    // Start is called before the first frame update
    //void Start()
    //{
        //
        //agent = GetComponent<UnityEngine.AI.NavMeshAgent>();
    //}

    // Update is called once per frame
    //void Update()
    //{
        //
        //float distance = Vector3.Distance(transform.position, Player.transform.position);

        //
        //if (distance < EnemyDistance)
        //{
            //
            //Vector3 dirToPlayer = transform.position - Player.transform.position;
            //
            //Vector3 newPos = transform.position + dirToPlayer;
            //
            //agent.SetDestination(newPos);
        //}
    //}
}
